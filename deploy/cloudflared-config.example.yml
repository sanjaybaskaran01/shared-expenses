tunnel: replace-with-tunnel-id
credentials-file: /absolute/path/to/replace-with-tunnel-id.json

ingress:
  - hostname: tally-api.example.com
    service: http://127.0.0.1:3000
  - service: http_status:404
