{
  "name": "shared-expenses",
  "private": true,
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "overrides": {
    "brace-expansion": "5.0.8"
  },
  "scripts": {
    "dev": "bun --filter @expenses/server --filter @expenses/web dev",
    "dev:server": "bun --filter @expenses/server dev",
    "dev:web": "bun --filter @expenses/web dev",
    "test": "bun test",
    "typecheck": "bun --filter @expenses/protocol typecheck && bun --filter @expenses/server typecheck && bun --filter @expenses/web typecheck && tsc --noEmit -p scripts/tsconfig.json",
    "validate": "bun run typecheck && bun run test",
    "build": "bun --filter @expenses/protocol build && bun --filter @expenses/web build && bun --filter @expenses/server build",
    "build:release-artifacts": "bun scripts/build-release-artifacts.ts",
    "release": "bun scripts/release.ts",
    "assets": "bun apps/web/scripts/generate-assets.ts",
    "recovery:new-generation": "bun scripts/rotate-generation.ts",
    "security:audit": "bun scripts/security-audit.ts",
    "check": "bun run security:audit && bun run validate && bun run build"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2",
    "wrangler": "4.114.0"
  }
}
